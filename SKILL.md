---
name: doc-bilingual-reader
description: >
  把英文文档（论文 PDF / Word）翻译成中文，并生成一个「原文在上、逐句中文对照在下」的
  单文件 HTML 阅读器。英文单词可点击查释义与音标、可朗读，图表内的文字也可点击。
  产出物完全离线自包含，可转发。当用户要求「翻译 PDF/Word」「英文文献双语对照」
  「逐句对照翻译」「生成可点词查义的翻译文档」时使用。
agent_created: true
---

# 文档双语对照阅读器

把一份英文文档变成**一个自包含的 HTML 文件**：保留原版排版，每句英文下方直接给出中文，
每个英文单词可点击查看释义与音标，图表里的文字同样可点。产物断网可用、可直接转发。

## 核心约束（先读，决定一切设计）

| 约束 | 说明 |
|---|---|
| **产出是单个 HTML 文件** | 图片 base64 内联、词典内联、无外部 CDN、无网络请求 |
| **翻译由当前模型完成** | 不调用外部翻译 API。模型自己逐句翻，保证术语一致 |
| **译文不得留占位符** | `build_html.py` 有硬性审核，缺句/占位/未翻译会直接构建失败 |
| **原文要有文字层** | 可选中文字的 PDF / docx，或带 OCR 文字层的扫描件。**纯图片扫描件（0 字符）拒绝**，不要硬做 |
| **公式识别为文本** | 公式不走图片裁剪，识别成文本/LaTeX 输出 |
| **图表内文字可点** | 图片区域用 OCR 生成透明热区覆盖层 |
| **查词全部在生成时完成** | 文章词汇有限且已知，构建期一次性查好内联，点击时零查询、零延迟 |

## 环境准备

用托管 venv 的 Python（Windows 路径示例，其他平台换成对应的 bin/python）：

```bash
PY="C:/Users/zhang/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
"$PY" -m pip install pymupdf pillow numpy rapidocr-onnxruntime
```

- `pymupdf` 必需（解析 PDF、抽图、定位）
- `pillow` / `numpy` / `rapidocr-onnxruntime` 用于图表内文字 OCR（纯 pip，无需系统级 tesseract）
- 若 OCR 依赖装不上，跳过 OCR 步骤即可，文档仍可用，只是图内文字不可点

## 执行流程

### Step 0 — 判断输入是否可用

打开文档，确认有文本层：

```bash
"$PY" -c "import pymupdf; d=pymupdf.open('in.pdf'); p=d[0]; print(len(p.get_text().strip()))"
```

判据分三档（详见「扫描件 PDF 的额外处理」第 1 小节）：

- 输出 **`0` 字符** → **纯图片扫描件**，无文字层，停止；建议先做 OCR 或换文字版
- 输出非 0，但**每页都有满页栅格图** → **扫描图 + OCR 文字层**，可以做，但必须走
  `scripts/scan/` 那条链路（16 个专用脚本 + 三个门禁，一个都不能少）
- 有文字层、**没有**满页图 → **原生矢量文本**，按下面的常规流程走

### Step 1 — 抽取几何模型

```bash
"$PY" scripts/extract.py input.pdf -o model.json --stats
```

产物 `model.json` 结构：

```json
{
  "source": "paper.pdf", "page_count": 12, "unit": "pt",
  "pages": [{
    "page": 1, "width": 595.0, "height": 842.0,
    "elements": [
      {"kind": "text", "bbox": [x0,y0,x1,y1], "text": "...",
       "font": "Times-Roman", "size": 10.5,
       "style": {"italic": false, "bold": false, "serifed": true},
       "sentences": ["第一句。", "第二句。"]},
      {"kind": "formula", "bbox": [...], "text": "L = ∑ α_i log p_i", "sentences": []},
      {"kind": "image", "bbox": [...], "image_b64": "iVBOR...", "image_ext": "png"}
    ]
  }]
}
```

- `bbox` 单位是 PDF 点，原点左上、y 向下，渲染时直接当 px 用
- `sentences` 已做好切句，**每句对应一条译文**
- 用 `--stats` 检查：如果 `formula_spans` 为 0 但文档里明明有公式，去看 `text_spans` 里有没有混进去的，必要时手工调整译文对齐

**元素 id 规则：`"<page>#<element_index>"`**，这是后续所有环节的对齐主键。

### Step 1b — LaTeX PDF 必须做的预处理

如果 PDF 是 LaTeX 排版（论文/讲义，字体是 Computer Modern），**先跑这一步再做别的**：

```bash
"$PY" scripts/merge_lines.py model.json -o merged.json --formulas formulas.json
```

它做四件事：展开连字 → 合并同行碎片 → 折合分数 → 段落重排，
输出仍是同一套 schema，后续步骤把 `model.json` 换成 `merged.json` 即可。

- 首次跑可以不带 `--formulas`，看告警找出线性化失败的公式
- 修正表**按原文文本键控**（不是元素 id），见 `references/formulas.example.json`
- 跑完检查输出里的 `elements` 数量与 `text`/`formula` 统计，确认没有残留碎片
- **之后的译文必须按内容重新对齐**（见「LaTeX 排版 PDF」一节），不能靠 id 推算

非 LaTeX PDF（普通 Word/网页导出）跳过这一步。

同时参考 `references/terms.example.json` 的格式准备领域术语表。

### Step 2 — 模型逐句翻译（关键步骤，由你完成，不可跳过）

读 `model.json`，对**每个 `kind == "text"` 元素**，按 `sentences` 数组逐句翻译成中文。

> **这一步是流水线的核心，不是可选项。** `build_html.py` 会对译文做硬性审核，
> 缺句、数量不符、留占位符、或译文与原文相同，都会导致构建失败。
> **不要写 `【待译】`、`TODO` 之类的占位符**，也不要跳过任何句子。
> 你就是翻译引擎——直接用你的语言能力把每一句翻出来。

产出 `translations.json`：

```json
{
  "1#0": ["注意力机制就是你所需要的一切"],
  "1#1": ["主流的序列转导模型基于复杂的循环神经网络或卷积神经网络。"],
  "1#2": ["我们提出 Transformer，它完全建立在注意力机制之上。"],
  "1#3": ["这比此前最佳结果高出 2.0 分。"]
}
```

规则：

1. **译文数组长度必须等于 `sentences` 长度**，一一对应。不能合并、不能拆开。
   先数一遍 `len(sentences)`，确保写出来的是同样条数
2. **公式元素另存 LaTeX**，键名加后缀：`"1#7::latex": "L = \\sum_i \\alpha_i \\log p_i"`
3. **公式保持原样不翻译**，只做规范化（把 `·`、`−` 等恢复成正确的数学符号）
4. 术语要**全篇一致**：同一个词在全文用同一个译法。第一次遇到时记下来，后续复用
5. 译文要通顺，符合中文表达习惯，不要逐字硬译
6. **不要翻译公式里混着的文字**，也不要翻译参考文献编号、图表编号
7. 长文档**分批处理**：按页处理，每翻完若干页就把结果写盘合并，避免上下文过长
8. 翻完后**自己抽查一遍** `translations.json`，确认没有漏项和空串

同时产出 `terms.json`——**文档里出现的、词典可能查不到的专业术语**：

```json
{
  "transformer": {"p": "trænsˈfɔːmə(r)", "t": "n.", "m": "变换器（本文指一种神经网络架构）"},
  "self-attention": {"p": "self əˈtenʃn", "t": "n.", "m": "自注意力机制"}
}
```

这一步很重要：通用词典对论文术语的释义往往不贴切，由你补上领域义项，
点词体验才有意义。**优先补全文档里反复出现的技术名词。**

### Step 3a — 预查全部单词释义（**核心步骤，决定点词体验**）

**思路**：一篇文章的词汇是有限且已知的，所以在生成阶段就把**每个会出现在页面上、
可能被点击的单词**查好，写进 HTML。读者点击时是纯本地查表——即时、离线、永不失败。

```bash
"$PY" scripts/prefetch_dict.py model.json -o dict.json \
  --terms terms.json \
  --cache .dictcache.json \
  --workers 4
```

脚本会：

1. 扫描文档，收集所有可点击单词（自动跳过 the/of/and 这类功能词）
2. 按优先级解析：`terms.json` > 本地 ECDICT > 缓存 > 联网查询
3. 联网时走**服务端请求**（百度 `fanyi.baidu.com/sug` 优先，有道 `suggest` 兜底）——
   服务端没有 CORS 限制，这两个接口都能正常返回中文释义
4. 缓存查询结果，重跑时不再重复请求
5. 输出版本化词典，并报告覆盖率

输出示例：

```json
{
  "vocabulary": 842,
  "resolved": 839,
  "coverage": 99.6,
  "unresolved": 3,
  "size_kb": 96.4
}
```

**必读注意事项**：

- **缩写词不会联网查**（BLEU、WMT、RNN 这类）。通用词典对它们的释义是错的——
  BLEU 会被查成"法国蓝纹奶酪"。脚本会跳过它们并提示你**必须在 `terms.json` 里补上**，
  否则显示"未收录"
- **覆盖率低于 95% 会告警**。这时候要么补 `terms.json`，要么用 `--ecdict` 加本地词库
- 有 ECDICT 时优先用它，无速率限制且可离线：`--ecdict ecdict.csv --max-rank 60000`
  （https://github.com/skywind3000/ECDICT ）
- 查询用多线程（`--workers 4`），1000 词大约 1–2 分钟。别把 workers 调太高，会被限流
- **单字母不会被收录**（`is_lookup_word` 规则）。`e.g.`/`i.e.` 会被切成 `e`+`g`，
  查出来是"英文字母表的第 5 个字母"这种废话，所以长度 <2 的词直接不点。
  停用词（the/of/and…）同理。它们仍然照常显示，只是不可点击

### Step 3b — 图表内文字热区（有图片时才做）

```bash
"$PY" scripts/ocr_hotspots.py model.json -o hotspots.json --min-conf 55
```

- 对每个 `kind == "image"` 元素做 OCR，输出词级坐标（相对图片像素）
- 没有 OCR 依赖时此步失败可跳过，传 `--hotspots` 时不存在文件会自动忽略
- `--min-conf 55` 是置信度门槛，图表文字模糊时可降到 45

### Step 4 — 生成 HTML

```bash
"$PY" scripts/build_html.py model.json translations.json dict.json \
  -o out.html \
  --hotspots hotspots.json \
  --title "论文标题 · 双语对照"
```

**构建会先过译文审核**，任一情况都会让构建失败（退出码 3）：

- 某个 text 元素没有译文
- 译文条数和原文句数不一致
- 译文里含 `【待译】`/`TODO` 之类的占位符
- 译文和英文原文一字不差（说明根本没翻）

失败时脚本会列出所有问题元素 id。**回去把 translations.json 补全再跑**，
不要用 `--allow-incomplete` 绕过（那个开关只用于调试排版）。

产出的 `out.html` 就是最终交付物。

### Step 5 — 必须验证

**不要生成完就交付。** 三道验证，前两道是自动门禁，第三道要你看图：

```bash
# 1) 模型层：11 类扫描件缺陷，P0 非空则退出码 1
python scripts/audit_scan.py <model.json> -z <zh.json>

# 2) 渲染层：重叠 / 空块 / 左右栏是否并排 / 外部请求 / JS 报错 / 点词
NODE_PATH=<puppeteer-core 目录> node scripts/verify_html.js <out.html>
```

第二道需要一个本地 `puppeteer-core`（`scripts/verify_html.js` 里可用
`EDGE_PATH` / `CHROME_PATH` 指定浏览器）。它查的是**只有排版之后才存在**的缺陷：
模型可以完全正确，页面仍然渲染成一条竖排（gutter 判错就是这样），
而"左右栏是否并排"只能从坐标算——判据是两栏的纵向覆盖必须重叠 ≥60%；
某页 31% 就是塌了，94% 才对。

第三道，截图看图确认：

```bash
node -e "
const p=require('puppeteer-core');
(async()=>{
  const b=await p.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:'new'});
  const pg=await b.newPage();
  await pg.setViewport({width:1100,height:1000,deviceScaleFactor:2});
  await pg.goto('file:///ABSOLUTE/PATH/out.html',{waitUntil:'networkidle0'});
  await pg.screenshot({path:'check.png',fullPage:true});
  await b.close();
})();
"
```

看图确认三件事：

1. **中文和英文没有重叠** —— 有重叠说明 `relayout_page` 的高度估算不够，调大 `SAFETY`
2. **中文没有溢出页面右边** —— 溢出说明容器宽度偏窄
3. **公式位置正常** —— 不应该有明显错位

再验证交互（点词、开关中文、发音）无 JS 报错。

## 已知限制（交付时要主动告知用户）

- **扫描件不支持**：纯图片 PDF 没有文本层，必须先 OCR
- **扫描件分三档**：纯扫描图（0 字符）直接拒绝；扫描图 + OCR 文字层可以做（见上节）；别把两者混为一谈
- **`build_html.py` 的译文审核是硬门禁**（不过就退出码 3）：报「译文为空 / 条数不符」时，
  **先查英文是否被上游重排切碎，不要急着补译**。
- **保留的封面栅格图没有译文**：期刊首页的刊头/标题/作者/致谢脚注多是位图，
  OCR 常整块漏掉，所以那一条是最初也是最后一块"未译区域"。
  `audit_scan.py` 会给 P2 提示，**交付时要主动问用户**：保持影像、补一份图文对照、还是去掉。
  实测 64 个「译文为空」的真因全在英文侧（参考文献被缩写句点撕碎）。
  **不要用 `--allow-incomplete` 绕过**，那个开关只用于调试排版
- **双栏论文是难点**：`extract.py` 按 y 坐标排序，双栏排版可能把左右栏句子交错。
  遇到双栏文档，检查 `fonts` 和元素顺序，必要时按 x 坐标先分组再排
- **公式转文本有出错风险**：复杂公式（矩阵、多行对齐、嵌套积分）反推可能失真。
  简单公式一般没问题。发现错的，在 `translations.json` 的 `::latex` 里手工修
- **中文比英文短**：对照后行距会变化，页面整体会变长，这是正常的
- **`measure_lines` 是估算**：不是真实字体度量，极端字体下可能有 1 行误差
- **缩写词依赖 `terms.json`**：BLEU/WMT/RNN 这类不会联网查（词典释义是错的），
  必须由模型补上，否则这些词显示"未收录"

### 扫描件 PDF 的额外处理（逐层排查，别一上来就放弃）

#### 0. 先读这一节：流程、脚本位置、两个门禁

扫描件的难点**不在翻译，在版式数据的清理**。下面十几条坑里，凡能由规则判定的都已落成
代码；剩下的是**执行顺序**问题——顺序错了不会抛异常，只会静默产出更差的文本，
而这一篇的返工全部来自这里。

**脚本位置**：所有扫描件专用脚本在 `scripts/scan/`（`reflow_scan.py`、`clean_scan.py`、
`fix_dropcap.py`、`fix_gutter_joins.py`、`fix_final.py`、`fix_headers.py`、
`fix_pagenums.py`、`crop_scan_cover.py`、`align_segments.py` 等 16 个）。
通用入口 `extract.py` / `build_html.py` / `prefetch_dict.py` 仍在 `scripts/`。

**三个门禁，缺一不可**：

| 门禁 | 查什么 | 不过怎么办 |
|---|---|---|
| `build_html.py`（内置） | 每句原文是否都有对应译文；有占位符、译文与原文相同都算失败 | 退出码非 0，禁止 `--allow-incomplete` |
| `audit_scan.py <model> -z <zh>` | **模型层** 11 类缺陷：页脚页码残留、gutter 漏检、栏式塌陷、未裁切的满页栅格图、跨页重复的页眉碎片、双词干融合、孤儿句尾、空槽重叠… | P0 非空（退出码 1）→ 按提示修完再交付 |
| `verify_html.js <html>` | **渲染层** 6 类：元素重叠、空块、**左右栏是否并排**、外部请求、JS 报错、点词弹窗 | 任一 FAIL → 修完再交付 |

后两个是这次返工的直接产物。Mankiw 交付出来的三个缺陷（页码当正文、首页塌成单栏、
整行/半边交替）恰好都在前两个门禁的射程内——**当时它们还不存在**。所以：
**不要只跑覆盖率就交付。**

**执行顺序不可换**（三条都踩过）：

```
恢复被剪掉的正文  →  裁切栅格图  →  剥页脚页码  →  构建  →  审计
      (stage 18)      (stage 19)     (stage 20)
```

- **裁图在剥页码之后**会让首页封面从 327pt 涨回整页 696pt：裁图测的是"文字层没描述
  哪些纵向带"，页码属于文字层，先剥掉它那条带就变宽、把页码自己的墨迹算进去（0.26%，
  刚好越过保留阈值）。
- **裁图在恢复正文之前**也会偏：恢复正文会让文字层起点上移，裁切边界随之移动。
- **剥页码在最后但不能再晚**：它必须在构建之前。

**遇到新论文先跑 probe，不要靠猜**：

```bash
python scripts/scan/fix_headers.py <model.json> <zh.json> --probe
```
它会列出"在 ≥3 页开头重复出现的短语"——即该刊的页眉碎片（英文侧 + 中文侧）。
重复是通用信号：正文句子不会在三页里以同样文字开头。Mankiw 的 probe 直接吐出
`'# Journal of Economic'`、`'Mankiw: A Quick Refresher'`、`'Course in Macroeconomics'`
三片，不需要预先知道刊名。

**页眉的中文译法不要让模型去列**。译者怎么译是他们的自由，而且同一篇里都不统一：
Mankiw 把 "Course in Macroeconomics" 分别译成「速成课程」「快速复习」「课程」。
`fix_headers.py` 会**从文档里自己学**（只扫"英文首句确实带页眉特征"的元素，
再取跨元素公共前缀），所以只传 `--en-piece` 就够，中文侧不用传。
`--no-learn-zh` 可关掉。

#### 1. 三档定性（先判断路线）

先分三档定性，路线完全不同：

| 判据 | 档位 | 路线 |
|---|---|---|
| `get_text()` 输出 `0` 字符 | **纯扫描图** | 无文字层，必须外部 OCR，**直接拒绝**并建议装 Tesseract 或另找文字版 |
| 有文字层，但每页都有**满页栅格图** | **扫描图 + OCR 文字层** | ✅ 可做，见下 |
| 有文字层，无满页图 | 原生矢量文本 | 常规流程 |

#### 2. 坑位详解（原理参考，编号与正文门禁对应）

**⚠️ 第一坑：满页扫描图会把文字层整个吃掉。**
`extract.py` 原本的 `_covered_by_image` 判定「文字被图片覆盖 ≥60% 即视为重复」而丢弃 ——
对满页扫描图这恰好把**真正的文字层全扔了**（实测 Mankiw 21 页抽出 0 个 text span，
而原 PDF 每页有 2445+ 字符）。
修复：满页图（面积 ≥0.8 页面积）视为扫描底图，**不再遮挡**文字；只有小图才允许遮蔽。
（试过按像素方差判「平铺填充」，失败 —— 扫描图是有效灰度图，方差正常，已回滚。）

**⚠️ 第二坑：双栏 OCR 把一行跨栏合并成一个 span。**
文字层里一行可能是 `"左栏尾  右栏首"`。按 y 排序会左右交错。做法：
检测 gutter（词间宽间隙的 x 中心聚类，期刊通常稳定在页宽 1/2 附近），
然后**先输出全部左栏（上到下）、再全部右栏**。

**⚠️ 第三坑：重排会把页眉黏进正文首句。**
页眉（如 `Vol. XXXVIII (December 1990)`）单独成行、紧贴正文首行、且同栏，
段落黏合阶段会把它接到该页第一句上。按正则从元素**开头**剥离即可（中英两侧都要剥）。

**⚠️ 第四坑：缩写句点撕碎参考文献。**
`re.split(r"(?<=[.!?])\s+")` 会把 `Quart. J.` / `Amer. Econ. Rev.` / `J. Polit. Econ.`
的缩写句点当句末，一条文献被撕成 3–8 个伪句 → 译文侧出现大量空槽。
**别用「缩写密度」判定参考文献**——正文脚注同样密集，一定误伤。
可靠信号是**结构**：双栏期刊里参考文献是「填满整页的满宽条目」，
其他元素都是单行正文或窄脚注片段。所以用
**「宽度 ≥ 60% 栏宽」+ 显式指定页码** 双条件，把该页满宽条目的 `sentences` 重写为整条一项。

**⚠️ 第五坑：OCR 会把逗号读成句号。**
如 `and low income,. it lowers the demand` —— 一句被劈成两句。用精确子串锚定修复，
不要写通用启发式。

**OCR 常见字错**（可建替换表，注意大小写都换）：
`jinancial/financial`、`jirm/firm`、`Keyndsians/Keynesians`、`unernployment/unemployment`、
`ectations/expectations`、`hercowitz/Hercowitz`、`malinvaud/Malinvaud`。
**首字下沉**（drop cap）常丢首字母：`WENTY`→`TWENTY`、`HIS`→`THIS`、`HAT`→`THAT`，
且因 y 坐标偏低会被排到句子之后 —— 建议直接按精确子串重排，别用启发式。

**⚠️ 第六坑：词替换表不加左边界，会把它要修的词改坏。**
片段词表里写了 `"acroeconomics" → "macroeconomics"`（首字母被首字下沉吃掉），
但替换没加 `\b` 左边界 —— 而 `macroeconomics` 本身就含 `acroeconomics`，
于是它被替换成 `m` + `macroeconomics` = **`macmacroeconomics`**，全文 57 处；
同理 `microeconomic` → `micmacroeconomic`。
教训：**这类片段词表的正则在两侧都要 `\b`**，否则是静默污染 —— 不改坏之前毫无迹象。

**⚠️ 第七坑：页眉是「两半」，正则锚在整行只会剥掉一半。**
页眉通常横跨页宽（`1646  Journal of Economic Literature, Vol. XXVIII (December 1990)`），
gutter 切分时被**劈成左右两半**，各自黏到该栏正文首句，所以**每页有两处伤**：
左栏 `Mankiw: A Quick Refresher ployment observed ...`、
右栏 `Course in Macroeconomics 1653 First, real business cycle ...`。
若正则写成 `^Vol\.\s*X+...`（锚在整行字面量），只能命中右半，左半 20 处全部漏网。
做法：**列出全部半片形态**（页码 + `Journal of Economic Literature,`、
`Mankiw: A Quick Refresher`、`Course in Macroeconomics <页码>`），
循环从元素开头逐片剥离，**中英两侧都要剥**（译者会把垃圾一并翻译：
`1646 《经济文献杂志》（Journal of Economic Literature），...`）。
判据用「头部 70 字符内出现页眉特征」再动手，别对全文档跑
`^\s*\d{3,4}\s+`（会吃掉正文里合法开头的数字）。

**⚠️ 第八坑：跨页/跨栏断句留下「孤儿句尾」。**
类型可被 OCR 和 gutter 切开的句子，句尾会孤零零出现在下一页/另一栏的开头，
如 `sions located in the labor market or in the goods market?`（头是上一栏末尾的
`...high unemployment in reces-`）。**关键洞察：译者往往已经把整句译全了**，
所以正解是**把句尾还给前一句**（前句以 `-` 结尾就直接拼接），而不是另写一条译文。
处理前先打印「首句以小写字母开头的元素」清单核对。

**⚠️ 第九坑：「剔除存根页」会连正文一起剔除，且事后无迹可寻。**
`clean_scan.py --drop-stub-pages` 之后如果再做一次手工剪枝（清理段落黏合阶段
扫进来的 JSTOR 残片），**剪掉的元素里可能混着真正文** —— 因为段落黏合早已把
正文和它同行的页眉/脚注块**融成了一个元素**，剪枝分不出来。
实测：Mankiw 第 1 页的 `Introduction` 小节标题 + 右栏开头三句正文被当成残片删掉。
**必须保留剪枝前的模型**（如 `Mankiw.model.json`），并写脚本逐元素 diff 证明
「剪了什么」，否则内容丢失不可逆、也查不出来。恢复时从剪枝前模型 + PDF 文字层取原文，
不要把译文连正文一起造。

**⚠️ 第十坑：首字下沉丢字母，还会打乱词序。**
不只是 `WENTY`→`TWENTY` 这种拼写错。首字下沉的剩余小体大写片段
（`TWENTY YEARS AGO,`）y 坐标比同行其余部分偏低，重排时会**被排到句子中间**：
`it was easier being TWENTY YEARS AGO, a student of macroeconomics.`
正确顺序是 `TWENTY YEARS AGO, it was easier being a student of macroeconomics.`
**旁证**：中文译文一直是正确的 —— 英文被排错时，先看中文对不对，中文对就说明错在英文侧。

**⚠️ 第十一坑：封面裁切必须在「未裁切」的模型上跑。**
`crop_scan_cover.py` 用「图片面积 ≥ 0.8 页面积」判定满页扫描图。
拿**已经裁过**的模型再跑一次，页面只剩半张图 → 判定为普通插图 → **整页跳过、静默不裁**。
表现是「明明跑了裁切，边界却纹丝不动」。改模型后要重跑裁切时，
必须从裁切前的那一步（`Mankiw.body5.json`）重新往后走。
（另一条顺序约束见第十五坑：裁图还要排在剥页码之前。）

**判定要不要保留底图：不要用「有没有未译内容」这种模糊说法。**
可测量的判据是：**算文字层未覆盖的纵向带 → 在栅格上量那条带的墨迹比例**。
实测 Mankiw 第 1 页顶部未覆盖带墨迹 **5.6%**（刊头/标题/作者/致谢脚注，
OCR 全丢了）→ 保留；其余页上下页边墨迹 **0.00%** → 丢弃。
页边也是「未覆盖带」，只有量墨迹才能把空白页边和「漏掉的封面」分开。
**副作用**：把某段恢复成文字后，文字层起点会上移，裁切边界随之移动 —— 顺序是
**先恢复文字，再跑裁切**。
补救用 `(mac|mic)(mac|mic)(?=ro)` → 只保留第一个词干
（`mac|macroeconomics`→`macroeconomics`，`mic|macroeconomic`→`microeconomic`），
改完**必须自校验「双词干计数 = 0」**。
注意这类词不在词典里，所以**查词覆盖率报告（`unresolved` 列表）是发现它的最好工具** ——
它一次就点出了 `macmacroeconomics`/`micmacroeconomic`/`inconsistentcy`。

**⚠️ 第十二坑：双栏版式必须「每栏一个下推游标」。**
插入中文后要把元素往下推防重叠。若只用一个全局 `last_bottom`，双栏页会退化成
一条竖排流：左块 → 右块 → 下一行左块 → …，结果是**任何高度上只有一栏有内容、
另一栏必然空白**，且页高约等于两栏之和。实测 504pt 双栏页：页高 3409px，
而两栏各只有约 1350px 墨迹。
正确做法：按 gutter 判定元素属左栏 / 右栏 / 跨栏，**每栏各自维护游标**，
跨栏元素（页眉、通栏标题）同时阻塞两栏。修后页高 3409 → 1481px，两栏并排填满。
自检方法：渲染后取每页各元素的 `top`，看**同一高度附近左右两栏是否都有元素** ——
若两栏的 `top` 序列完全不重叠，就是这个 bug。

**⚠️ 第十三坑：满页扫描图既不该整页留，也不该整页丢。**
- 整页留：每页顶部多出一大块未翻译原图（读者以为漏译），且占掉 8.4MB / 9.4MB 体积。
- 整页丢：**封面会消失**。刊头、标题、作者、致谢脚注多用大字号或斜体，
  OCR 常整块漏掉。实测该刊首页文字层从 696pt 的第 350pt 才开始，上半页只有图里有。

正确做法是**按页算「文字层未覆盖的纵向带」+ 量该带的实际墨迹**：
- 墨迹 ≥ 0.2% → 确实有内容，把图**裁到该带**保留（见 `crop_scan_cover.py`）
- 墨迹 ≈ 0 → 只是页边空白，整页丢弃

实测 15 页里只有首页需要保留（裁到 0–350pt，104KB），其余 14 页墨迹全是 0.00%。
`build_html.py --scan-images auto` 用**垂直覆盖率 ≥ 70%** 作兜底判据 ——
不能用字数阈值：首页字数并不少，但覆盖率只有 44%，按字数会误丢封面。

**顺带教训**：标题页不在文字层里时，很容易把论文认成另一篇。
**先读封面栅格确认标题/作者/出处，再写 `--title`。**
本文实际是 N. Gregory Mankiw《A Quick Refresher Course in Macroeconomics》
(JEL 28(4), Dec 1990, pp. 1645–1660)，而早期版本误写成了另一篇的标题。

**⚠️ 第十四坑：页码（folio）是页眉页脚里唯一漏网的一类，而且会改写整页栏式。**
页眉剥除脚本通常按「关键词」匹配（`Journal of Economic Literature,`、`Vol. XXVIII`、
刊名缩写），**页码是纯数字、不含任何关键词，于是全部漏网**。Mankiw 15 页留下 16 个
（`1645`…`1660`，且首页页脚那两个被 OCR 读成 formula / text 各一份，叠在同一位置）。
它有两重危害：

1. **它是全文唯一没有中文的文本。** `audit_translations` 本来就豁免「纯数字」
   （把页码译成中文是噪声），所以它渲染成一行光秃秃的数字、下面没有译文 ——
   读者的第一反应就是「这段为什么没译」，而它其实根本不是正文。
2. **它曾经会污染 gutter 检测。** 首页页码在页脚居中，x=241：**落在左栏的 x 区间内，
   却在中线 45% 的右侧**，于是被当成右栏的起点，gutter 从 261 被拉到 241。
   后果是左栏三块（右缘 244）全部「跨过」这条假 gutter → 判为跨栏元素 →
   既按跨栏顺序下推、又被放宽到 380pt 跨栏宽，整页从双栏塌成一条 1600px 的竖排。
   **根因已经堵掉**（见第十五坑：分界移到页面中心后，页码归左侧，不再参与右栏起点），
   所以现在残留页码只会造成上面第 1 条——一个碍眼的未译数字，不会再毁版式。

判据要四个条件同时成立：**kind 属 text/formula + 文本匹配 `\d{1,4}` +
宽度 < 页宽 12% + 位于页高上下 12% 的页边带内**（只按「中线右侧」会误吃正文里的数字），
见 `fix_pagenums.py`；`audit_scan.py` 的 `check_folio` 用同一组判据把它报成 P0。
剥除时**保留元素槽位**（只清 text/sentences + 删对应译文条目），span id 才不会错位。
配套地 `build_html.py` 要让空元素**既不绘制也不占位**（`_blanks()`），
否则页脚会留下一道 13px 的空白缝。

**⚠️ 第十五坑：gutter 检测在「首页形态」下会整体失效 —— 用「左右纵向重叠」兜底。**
`_detect_gutter` 原本只认两个信号：右栏文字起点在中线右侧 + 右栏纵向跨度 ≥ 60% 页高。
第二个信号本意是排除「标题页右上角的作者/单位行」，但它**把期刊论文首页一起排除了**：
首页上半是刊头、标题、作者、致谢脚注（OCR 常整块漏掉），双栏正文被挤到下半页，
右栏只跨 30% 页高 → 判为单栏 → 整页共用一个游标 → 左栏块被放宽到全页宽。
表现就是**同一页里「有的块占一整行、有的块只占半栏」交替排布**，页高也接近两栏之和。

正确的兜底判据是**纵向重叠**：真正的栏一定与另一栏**共享一段纵向区间**；
而单栏页里「偏右的行」只会出现在正文的上方或下方，永远不会与正文并排。
实测 Mankiw 首页重叠率 100%、其余 14 页 >90%、标题页块约 0%，阈值取 0.75。
判据顺序：先试原纵向跨度信号 → 失败则测与左栏的重叠 → 两者都不满足才算单栏。

**同一处还有一个更隐蔽的陷阱：左右分界不能是 45%，右栏起点不能用中位数。**
两个都试过，两个都错，方向相反：

- **分界 45%（=226pt）太靠左**，把首页页脚那个 x=241 的页码划进了「右栏」，
  于是右栏起点变成 241 而不是 261，左栏三块全部看着「跨栏」→ 整页塌成一条竖排。
  分界必须放在**页面中心**（252pt）：页码 241 归左侧，与它所属的栏一致。
  分界越靠左，越容易把「页边/居中元素」误收进右栏——这个错误是**静默**的。
- **右栏起点取中位数**看着稳健，实则更糟：右栏各块的 x0 本来就有零点几到几点的抖动
  （261.35 / 261.82 / …），中位数落在它们中间，于是**中位数左边那些右栏块全被判成跨栏**——
  参考文献页 21 个元素里报出 9 个假跨栏。
  正确的是取 **min**：它是右栏的**下界**，所有右栏块都 ≥ 它，`_column_of` 才全部放行。
  前提是分界在中心，右栏里没有混进页边元素——两个修正必须同时用。

`audit_scan.py` 的 `check_gutter_missed` 与 `check_column_collapse` 就是这两条的哨兵：
前者用**独立**的重叠判据复算一遍，报「判为单栏但其实是双栏」；
后者报「左栏块越过 gutter」和「宽度 > 60% 页宽的文本块过多」。

**⚠️ 与第十一坑的顺序冲突：裁图必须在剥页码之前。**
`crop_scan_cover.py` 判断的是「文字层没有描述哪些纵向带」，而页码属于文字层。
先剥掉它，首页下方那条带就从 `654–696pt` 变成 `637–696pt`，把页码自身的墨迹算进来
（实测 0.26%），刚好越过 0.2% 阈值 → 判为「有内容」→ 首页封面从 327pt 一路保留到
整页 696pt，封面体积 93KB → 293KB。完整顺序是
**先恢复文字（第十坑）→ 再裁图 → 最后剥页码**。

三条修完后的 Mankiw 交付：0.69MB / 15 页 / 395 句对 395 段中文 / 5486 可点词 /
1 张封面图 / 0 重叠 0 空块 0 散落数字 0 外部请求，首页恢复左右两栏并排
（页高 1646 → 1189px）。

### LaTeX 排版 PDF（论文/讲义）的额外处理

LaTeX 生成的 PDF（Computer Modern 字体）有一个不处理就没法用的坑：**每个数学原子
都是独立 span**，`extract.py` 会抽出一大堆碎片。实战结论（Econ 302 讲义，8 页）：

| 现象 | 处理 |
|---|---|
| `proﬁt` / `ﬁrm` / `deﬂator` 点击查不到 | LaTeX 把 fi/fl/ff 排成单字形 `ﬁ`(U+FB01)/`ﬂ`(U+FB02)/`ﬀ`(U+FB00)，**必须查词前展开为 ASCII**，否则词表里混入变体字形 |
| `GDP = C + I + G + NX` 被当正文 | 单元素桶也要跑公式重判；判定用「含 `=≥≤≠→∑∏∫∂` 且无功能词（the/of/is/that…）」 |
| 分数渲染成 `αY A(1 α)K L = − / α 1 1 α− −` | 分子/分数线/分母是三层，需上下配对折成线性 `A / B` |
| 行尾断句（`...and test` / `assumptions.`） | 段落重排：黏合完整句 + **去连字符**（`repre-`+`sentative`）+ 缩写保护（`vs.`/`e.g.`/`Fig.`） |
| 嵌套公式线性化仍失败 | 用**按原文文本键控**的覆盖表手工修正，不要按元素 id（合并后索引会变） |

关键顺序（错一步结果就不对）：

1. **先全模型展开连字** → 再做任何分词/查词
2. 合并同行碎片 → 分数折合 → 碎片吸收 → 段落重排 → 重新编号
3. 公式覆盖表在**重排之后**应用，且 `json.dump` 必须放在覆盖**之后**（放前面会导致修正不落盘）
4. 覆盖时置空的碎片元素要**过滤掉**，否则留白块

**重排后译文必须按内容重新对齐，不能靠 id 偏移。** 合并会让元素增删，索引整体位移；
即使发现"旧 id = 新 id + 常数"，也可能在某个碎片处断裂（实战中第 5 页就断了）。
正确做法是**按页内规范化文本做唯一匹配**——文本完全相同的元素互相配对，
实测 106/106 全中、零未匹配，比任何索引推算都稳。

### 页码/列表序号不要译文

单独占一行的纯数字（`5`、`6`）是页码或列表标记，加中文对照纯属噪音。
`build_html.py` 已内置：这类元素不输出译文，审核门禁也对其豁免。
注意豁免要同时改**两处**——`audit_translations` 和渲染函数，只改渲染会卡在门禁上。

## 为什么查词必须在生成时做（实测结论，不要改回运行时查询）

**曾经的错误设计**：点击时才去联网查。结果是读者点任何词都卡在"查询中…"，
最后什么也查不到。实测原因：

| 数据源 | 浏览器中调用 | 服务端调用 |
|---|---|---|
| 有道 `dict.youdao.com/suggest` | **403，无 CORS 头，被拒** | 200，正常返回中文释义 |
| 百度 `fanyi.baidu.com/sug` | 无 CORS 头，被拒 | 200，返回词性+多义项 |
| `api.dictionaryapi.dev` | 有 CORS 头 | 国内网络不可达 |

根因：产物是 `file://` 打开的本地页面，浏览器对跨域请求有严格限制，而中文词典服务
普遍不开放 CORS。**这是浏览器安全模型决定的，不是代码能绕过的。**

**正确设计**：既然文章词汇在生成时就完全已知，就**一次性查好写进文件**。
读者点击时是纯本地查表，即时、离线、可靠。附带好处：

- 点击响应从"等待网络/超时"变成瞬时
- 产物依然完全离线自包含
- 查询失败的问题在**生成阶段暴露**（覆盖率报告），而不是留给读者踩坑

**如果你（后续的模型）想"优化"成运行时查询，请先重跑上面的连通性测试。**

## 设计决策记录（改代码前先读）

- **为什么用绝对定位而不是流式重排**：PDF 每个字符都有坐标，复刻坐标才能保真。
  流式重排必然破坏多栏、图文环绕等版式
- **为什么要 `relayout_page`**：中文插在英文下方后，元素变高会撞到下一个元素。
  该函数按阅读顺序累加下推，保证永不重叠，页面高度按需增长。
  **注意它按栏维护独立游标**（见「第七坑」），单一全局游标会把双栏压成竖排流
- **为什么中间阶段的硬门禁不能直接判死整条链**：`fix_zh_final.py` 曾对空译文段
  返回 1，而它的下游 `fix_last3.py` 正是修这几段的步骤 —— 配合 `set -e`
  会让流水线在自救前自杀。现在的规则是：**结构性错配（段数不符）仍致命，
  已命名、且下一阶段专门负责的空段只报 NOTE**。真正的硬门禁只有
  `build_html.py` 的译文审核一处，它不可绕过，也不会漏
- **扫描图为什么用「未覆盖带 + 墨迹」判定而不是字数**：字数衡量的是"页面上有多少字"，
  而需要判断的是"图中是否还有文字层没有的内容"。封面页字数不少但覆盖只有 44%，
  按字数会误丢刊头与标题。墨迹测量直接回答后者（见「第八坑」）
- **为什么公式不裁图**：用户明确要求公式以文本输出，便于复制和检索。
  代价是复杂公式可能失真，已在限制中说明
- **为什么词典要内联**：要求产物断网可用。ECDICT 全量太大，用 `--max-rank` 截断，
  再由 `terms.json` 补齐论文专有术语
- **为什么热区用百分比定位**：图片按 `object-fit:contain` 缩放，
  百分比能让热区和图片同步缩放，不用重算像素
- **为什么短行要单独放宽宽度**：`thus` 这类独立成行的连接词，元素框宽度只有几十 px，
  中文译文塞进去会变成一行一个字。给 `.el.txt.short` 设 `width:auto` + 最小宽度 +
  `white-space:nowrap`，译文才能正常显示
